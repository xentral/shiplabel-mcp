services:
  shiplabel-mcp:
    build: .
    image: shiplabel-mcp:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Streamable HTTP MCP endpoint: http://127.0.0.1:8000/mcp
